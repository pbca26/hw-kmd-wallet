name: HWKMDElectronCI

on: 
  pull_request:
    types: [opened, reopened]

jobs:
  job-macos-build:
    name: Build (MacOS)
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '12'
        check-latest: true

    - name: Build GUI
      env:
        CI: ""
      run: |
        cd electron/ui
        npm install
        npm run build-electron

    - name: Build Electron App
      run: |
        cd electron
        npm install
        npm run dist
        tar -czvf hw-kmd-rewards-claim-desktop-osx.tar.gz dist

    - name: Upload hw-kmd-rewards-claim-desktop-osx.tar.gz as artifact
      uses: actions/upload-artifact@v1
      with:
        name: hw-kmd-rewards-claim-desktop-osx
        path: ./electron/hw-kmd-rewards-claim-desktop-osx.tar.gz

  job-linux-build:
    name: Build (Linux)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '12'
        check-latest: true

    - name: Build GUI
      env:
        CI: ""
      run: |
        cd electron/ui
        npm install
        npm run build-electron

    - name: Build Electron App
      run: |
        cd electron
        npm install
        npm run dist
        tar -czvf hw-kmd-rewards-claim-desktop-linux.tar.gz dist

    - name: Upload hw-kmd-rewards-claim-desktop-linux.tar.gz as artifact
      uses: actions/upload-artifact@v1
      with:
        name: hw-kmd-rewards-claim-desktop-linux
        path: ./electron/hw-kmd-rewards-claim-desktop-linux.tar.gz

  job-win-build:
    name: Build (Windows)
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '12'
        check-latest: true

    - name: Build GUI
      env:
        CI: ""
      run: |
        cd electron/ui
        npm install
        npm run build-electron

    - name: Build Electron App
      run: |
        cd electron
        npm install
        npm run dist
        tar -czvf hw-kmd-rewards-claim-desktop-win.tar.gz dist

    - name: Upload hw-kmd-rewards-claim-desktop-win.tar.gz as artifact
      uses: actions/upload-artifact@v1
      with:
        name: hw-kmd-rewards-claim-desktop-win
        path: ./electron/hw-kmd-rewards-claim-desktop-win.tar.gz